"""
MediaHub v2 — embedded Python runtime package.

Authority: ADR-007 (embedded Python via Chaquopy), ADR-008 (typed IPC).

Phase 2 Feature #5: exposes ``ping`` + ``verify_runtime`` (Pigeon-bound
functions) and the ``plugins`` subpackage (protocol + sandbox).
"""

__version__ = "0.1.0"
