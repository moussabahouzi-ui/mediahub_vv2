// =============================================================================
// MediaHub v2 — packages/tooling
// Authority: ADR-002 (lint config + tooling), ADR-015 (CI)
// =============================================================================
// Phase 0: tooling package holds lint config + custom_lint rules (Phase 1+).
// Not published; dev-only.

library mediahub_tooling;
