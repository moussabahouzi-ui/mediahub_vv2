# MediaHub v2 — packages/python_bridge
# Authority: ADR-008 (Pigeon typed IPC), ADR-002 (data-layer dep)
name: mediahub_python_bridge
description: "MediaHub v2 — typed Flutter <-> Python IPC bindings (Pigeon)."
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=3.7.0 <4.0.0"
  flutter: ">=3.29.0 <3.30.0"

dependencies:
  flutter:
    sdk: flutter
  # Shared DTO interfaces live in the domain (ADR-002).
  mediahub_domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  pigeon: ^22.7.0
  mocktail: ^1.0.4
